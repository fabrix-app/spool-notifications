'use strict'
/* global describe, it */
const assert = require('assert')

describe('ItemNotification Model', () => {
  it('should exist', () => {
    assert(global.app.api.models['ItemNotification'])
  })
})
